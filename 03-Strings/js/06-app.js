const produ = 'monitor de 20 pulgadas';

//repetir una cadena de texto...
const texto =' en promocion'.repeat(3);

console.log(texto);

console.log(`${produ} ${texto} !!!`);

//dividir un string

const avtividad ='soy el mejor programador de perficient';
console.log(actividad.split(' ')); //se divide por el espacio y lo hace un array