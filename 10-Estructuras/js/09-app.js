//OPERADORES TERNARIOS

const atuh = true;
const auth2= true;

console.log(auth ? 'si está autenticado' : 'no está autenticado');

//con if anidado

console.log( auth ? auth2? 'autenticado y puede pagar': 'no puede pagar': 'parte negativa el primer if');